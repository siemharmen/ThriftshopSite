@model ThriftshopSite.Models.EmployeeThriftshop

@{
    ViewData["Title"] = "Create";
    var shoplist = ViewData["ShopList"] as System.Collections.IEnumerable;
    var usera = ViewData["UserAccount"] as UserAccount;
    var user = ViewData["UserAccount"];
}

<h1>Create</h1>

<h4>EmployeeThriftshop</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form action="" method="post">
             <div class="form-group">
                <label class="control-label">Account</label>
                <input name="userGuid" value=@user class="form-control" name="Account" />
            </div>
              <div class="form-group">
                <label class="control-label"></label>
                <select name="thriftShop" asp-items="@(new SelectList(shoplist,"Name","Name"))" class="form-control"> </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
