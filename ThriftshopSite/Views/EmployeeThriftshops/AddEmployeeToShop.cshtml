@model ThriftshopSite.Models.EmployeeThriftshop

@{
    ViewData["Title"] = "Create";
    var usera = ViewData["UserAccount"] as UserAccount;
    var user = ViewData["UserAccount"];
    var ThriftShop = ViewData["ThriftShop"];
     var ThriftShops = ViewData["ThriftShops"] as IEnumerable<ThriftShop>;
}  


<h1>Create</h1>
      <div class="form-group">
                <label class="control-label"></label>
            </div>
<h4>EmployeeThriftshop</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form action="" method="post">
             <div class="form-group">
                <label class="control-label">Account</label>
                <input name="userGuid" value=@user class="form-control" name="Account" />
            </div>
              <div class="form-group">
                <label class="control-label"></label>
                <select name="thriftShop" asp-items="@(new SelectList(ThriftShops,"Name","Name"))" class="form-control"> </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
